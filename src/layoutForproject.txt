this project has 
    left panel
    right panel
        right panel will display multiple pages like overview and orders table
        overview page has 
            heading 
            main section 
            right bar with notification activities section etc ..
            rest details will be fond under the layout.txt file in overview's folder
        right panel also has a header on its top 
so understand the page is vertically divided in two parts
 left panel and right panel and the header is inside right panel so it will not appear over left panel 
 and the right panel is itself divided vertically in two parts the (mainsection and heading stacked onto each other) and the(right bar)
 so the header only appears just above the main section  and does not span over right bar and left panel  

